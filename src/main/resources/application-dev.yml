spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinema_db
    username: postgres
    password: post
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  thymeleaf:
    cache: false